// Dependencies
import React from 'react';
import { Jumbotron, Container } from 'reactstrap';

const Bar = () => (
    <Jumbotron>
        <p>On filters click highlight the selected item in the table</p>
        <p className="lead">Display status information here</p>
    </Jumbotron>
);

export default Bar;
